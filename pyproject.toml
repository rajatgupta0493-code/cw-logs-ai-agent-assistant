[project]
name = "cw-logs-ai-agent-assistant"
version = "0.1.0"
description = "Stateless AI agents for CloudWatch on AWS Lambda"
requires-python = ">=3.11"

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.ruff]
line-length = 88
target-version = "py311"
select = ["E", "F", "I"]
